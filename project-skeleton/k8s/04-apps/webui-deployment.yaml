apiVersion: apps/v1
kind: Deployment
metadata:
  name: webui
  namespace: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webui
  template:
    metadata:
      labels:
        app: webui
    spec:
      containers:
        - name: webui
          image: docker.io/training/webui:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
  # revision: 1
